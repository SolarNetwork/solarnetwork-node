# UCUM standard units used in SolarNetwork
#
# Format is `N.key = unit` where `N` is the count of base units, `key` is a unique ID
# used to distinguish between all units within the same base unit count, and `unit`
# is the associated UCUM unit.

std.2.Wh = W.h

alt.V.A{reactive} = var

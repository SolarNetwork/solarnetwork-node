title = GPIO Control
desc = Read and write digital IO on GPIO lines.

gpioDevice.key = GPIO Device
gpioDevice.desc = The GPIO device. Can be specified as a number, starting at <code>0</code>, \
	or a full system device path.

propConfigs.key = Control Configurations
propConfigs.desc = A list of control property configurations that determine how controls \
	are connected to GPIO ports.

gpioDirection.Input = Input
gpioDirection.Output = Output

gpioEdgeMode.Rising = Rising
gpioEdgeMode.Falling = Falling
gpioEdgeMode.RisingAndFalling = Rising and falling
